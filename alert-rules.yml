groups:
  - name: springboot-alerts
    rules:
      - alert: HighRequestRate
        expr: sum(rate(http_server_requests_seconds_count[1m])) > 1
        for: 30s
        labels:
          severity: warning
        annotations:
          summary: "High request rate detected."